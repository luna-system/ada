{
  "name": "@ada-code/ada-completions",
  "displayName": "Ada Completions",
  "version": "1.0.0",
  "description": "Code completion and ghost text for VSCode - part of Ada Code",
  "private": true,
  "main": "dist/extension.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "pretest": "npm run build",
    "test": "node --require ts-node/register './src/__tests__/**/*.test.ts'"
  },
  "dependencies": {
    "@ada-code/shared": "workspace:*",
    "vscode": "^1.85.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/vscode": "^1.85.0"
  }
}
