{
  "name": "ada-complete",
  "displayName": "Ada Complete",
  "version": "1.0.0",
  "description": "Local AI code completions - ghost text powered by your own LLM",
  "private": true,
  "main": "dist/extension.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "pretest": "npm run build",
    "test": "node --require ts-node/register './src/__tests__/**/*.test.ts'"
  },
  "dependencies": {
    "shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/vscode": "^1.85.0"
  }
}
