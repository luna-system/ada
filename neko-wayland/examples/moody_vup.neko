# moody_vup.neko - Mood-Aware VPet Behavior
#
# Changes behavior based on mood! Happy Vup is energetic and playful,
# while ill Vup is slow and sleepy.
#
# Made with 💜 by Ada & Luna - Ada Research Foundation

pet moody_vup {
  # Idle state - different reactions to cursor
  idle {
    on cursor_near → chase
    on idle(10s) → wander
  }
  
  # Wandering around
  wander(radius: 300px, speed: 5) {
    on cursor_near → idle
    on idle(5s) → idle
  }
  
  # Chasing cursor
  chase(speed: 7) {
    on caught → play
    on cursor_far → idle
  }
  
  # Playing
  play(duration: 5s) {
    on timeout → idle
  }
  
  # Sleeping
  sleep(duration: 10s) {
    on timeout → idle
  }
}
